<%
block('title', model.getTitle());
block('titleTranslation', 'none');
%>

<%- include(_view.getInternalTemplate('_form')) %>

<%- include(_view.get('_part/group/tabs'), {
    items: [{
        id: 'main',
        label: 'Main',
        active: true,
        content: _view.getInternalTemplate('group/main')
    }, {
        id: 'startingTransitions',
        label: 'Starting transitions',
        hint: 'Transitions starting from this state',
        content: _view.getInternalTemplate('group/startingTransitions'),
        loadable: true
    }, {
        id: 'finishingTransitions',
        label: 'Finishing transitions',
        hint: 'Transitions ending in this state',
        content: _view.getInternalTemplate('group/finishingTransitions'),
        loadable: true
    }]
}); %>